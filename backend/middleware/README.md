# Middleware de Tenant

## Descripción

Detecta automáticamente el tenant (cliente) de cada request HTTP.

## Métodos de Detección

1. **Header HTTP** (Recomendado para APIs)
2. **Subdominio** (Recomendado para web)
3. **URL Parameter** (Solo desarrollo)
